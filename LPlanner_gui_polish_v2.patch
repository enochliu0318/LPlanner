diff --git a/css/style.css b/css/style.css
index 856f440..327a327 100644
--- a/css/style.css
+++ b/css/style.css
@@ -30,9 +30,23 @@
   --font-sans: 'IBM Plex Sans', 'PingFang SC', 'Microsoft YaHei', sans-serif;
   --font-mono: 'IBM Plex Mono', monospace;
 
-  --radius-s: 3px;
-  --radius-m: 5px;
-  --shadow-card: 0 1px 0 rgba(43, 38, 32, 0.06);
+  /* 圆角：从「扁平」升级为「柔和精致」，同一比例贯穿全站控件 */
+  --radius-xs: 4px;
+  --radius-s: 6px;
+  --radius-m: 10px;
+  --radius-l: 16px;
+  --radius-pill: 999px;
+
+  /* 阴影：统一用墨绿色调而非纯灰，与主题色调一致，分四档层级 */
+  --shadow-xs: 0 1px 2px rgba(22, 40, 31, 0.06);
+  --shadow-sm: 0 2px 8px rgba(22, 40, 31, 0.08);
+  --shadow-md: 0 10px 28px rgba(22, 40, 31, 0.14);
+  --shadow-lg: 0 20px 52px rgba(22, 40, 31, 0.22);
+
+  /* 统一缓动曲线，让所有过渡动效手感一致 */
+  --ease: cubic-bezier(.4, 0, .2, 1);
+
+  --shadow-card: var(--shadow-xs);
 }
 
 * { box-sizing: border-box; }
@@ -70,14 +84,17 @@ button, input, textarea, select {
 }
 
 .app-rail {
-  width: 220px;
+  width: 224px;
   flex-shrink: 0;
-  background: var(--ink-green-900);
+  background: linear-gradient(165deg, var(--ink-green-900) 0%, #122019 100%);
   color: var(--chalk);
-  padding: 28px 20px;
+  padding: 28px 18px;
   display: flex;
   flex-direction: column;
-  gap: 28px;
+  gap: 26px;
+  box-shadow: var(--shadow-md);
+  position: relative;
+  z-index: 1;
 }
 
 .app-rail .brand {
@@ -86,37 +103,57 @@ button, input, textarea, select {
   font-weight: 700;
   line-height: 1.3;
   color: #fff;
-  border-bottom: 1px solid rgba(255,255,255,0.14);
+  border-bottom: 1px solid rgba(255,255,255,0.12);
   padding-bottom: 16px;
+  display: flex;
+  align-items: center;
+  gap: 8px;
+}
+.app-rail .brand::before {
+  content: "📖";
+  font-size: 18px;
+  filter: drop-shadow(0 1px 1px rgba(0,0,0,0.3));
 }
 
 .app-rail .brand small {
   display: block;
   margin-top: 6px;
   font-family: var(--font-sans);
-  font-size: 12px;
+  font-size: 11.5px;
   font-weight: 400;
-  color: rgba(242,237,224,0.55);
+  letter-spacing: 0.02em;
+  color: rgba(242,237,224,0.5);
 }
 
 .app-rail nav {
   display: flex;
   flex-direction: column;
-  gap: 4px;
+  gap: 3px;
 }
 
 .app-rail nav a {
-  color: rgba(242,237,224,0.85);
+  display: flex;
+  align-items: center;
+  gap: 8px;
+  color: rgba(242,237,224,0.8);
   text-decoration: none;
-  padding: 8px 10px;
+  padding: 9px 11px;
   border-radius: var(--radius-s);
   font-size: 14px;
+  border-left: 3px solid transparent;
+  transition: background .15s var(--ease), color .15s var(--ease), border-color .15s var(--ease);
+}
+
+.app-rail nav a:hover {
+  background: rgba(255,255,255,0.07);
+  color: #fff;
 }
 
-.app-rail nav a:hover,
 .app-rail nav a.active {
-  background: rgba(255,255,255,0.08);
+  background: rgba(255,255,255,0.1);
   color: #fff;
+  border-left-color: var(--pencil-red);
+  font-weight: 600;
 }
 
 .app-rail .rail-foot {
@@ -124,14 +161,15 @@ button, input, textarea, select {
   font-size: 12px;
   font-style: italic;
   line-height: 1.7;
-  color: rgba(242,237,224,0.55);
-  border-top: 1px solid rgba(255,255,255,0.12);
+  color: rgba(242,237,224,0.5);
+  border-top: 1px solid rgba(255,255,255,0.1);
   padding-top: 14px;
+  position: relative;
 }
 .app-rail .rail-foot .rail-foot-text {
   font-family: var(--font-serif);
   font-size: 12.5px;
-  color: rgba(242,237,224,0.7);
+  color: rgba(242,237,224,0.68);
 }
 
 /* ---------- 左栏：已打开教案的标签页 ---------- */
@@ -157,18 +195,18 @@ button, input, textarea, select {
   display: flex;
   align-items: center;
   gap: 6px;
-  color: rgba(242,237,224,0.85);
+  color: rgba(242,237,224,0.8);
   font-size: 13px;
   line-height: 1.4;
   padding: 7px 6px 7px 10px;
-  border-radius: var(--radius-s);
+  border-radius: var(--radius-xs);
   border-left: 3px solid transparent;
   cursor: pointer;
-  transition: background .12s ease;
+  transition: background .15s var(--ease), color .15s var(--ease), border-color .15s var(--ease);
 }
 
 .app-rail .rail-tab:hover {
-  background: rgba(255,255,255,0.08);
+  background: rgba(255,255,255,0.07);
   color: #fff;
 }
 
@@ -176,6 +214,7 @@ button, input, textarea, select {
   background: rgba(255,255,255,0.1);
   color: #fff;
   border-left-color: var(--pencil-red);
+  font-weight: 500;
 }
 
 .rail-tab-title {
@@ -195,7 +234,8 @@ button, input, textarea, select {
   font-size: 11px;
   line-height: 1;
   padding: 3px 5px;
-  border-radius: 3px;
+  border-radius: var(--radius-xs);
+  transition: background .14s var(--ease), color .14s var(--ease);
 }
 
 .rail-tab-close:hover {
@@ -230,42 +270,57 @@ button, input, textarea, select {
   border: 1px solid var(--border);
   background: var(--paper-card);
   color: var(--ink);
-  padding: 8px 14px;
+  padding: 8px 15px;
   border-radius: var(--radius-s);
   cursor: pointer;
   font-size: 13.5px;
   font-weight: 500;
   line-height: 1;
-  transition: border-color .12s ease, background .12s ease;
+  box-shadow: var(--shadow-xs);
+  transition: border-color .16s var(--ease), background .16s var(--ease),
+              box-shadow .16s var(--ease), transform .16s var(--ease), color .16s var(--ease);
+}
+.btn:hover {
+  border-color: var(--ink-green-700);
+  box-shadow: var(--shadow-sm);
+  transform: translateY(-1px);
+}
+.btn:active { transform: translateY(0); box-shadow: var(--shadow-xs); }
+.btn:focus-visible {
+  outline: 2px solid var(--ink-green-700);
+  outline-offset: 2px;
 }
-.btn:hover { border-color: var(--ink-green-700); }
 
 .btn-primary {
   background: var(--ink-green-800);
   border-color: var(--ink-green-800);
   color: #fff;
 }
-.btn-primary:hover { background: var(--ink-green-700); }
+.btn-primary:hover { background: var(--ink-green-700); border-color: var(--ink-green-700); }
 
 .btn-accent {
   background: var(--pencil-red);
   border-color: var(--pencil-red);
   color: #fff;
 }
-.btn-accent:hover { background: var(--pencil-red-dark); }
+.btn-accent:hover { background: var(--pencil-red-dark); border-color: var(--pencil-red-dark); }
 
 .btn-ghost {
   background: transparent;
   border-color: transparent;
   color: var(--ink-soft);
+  box-shadow: none;
 }
-.btn-ghost:hover { color: var(--ink); background: var(--kraft); }
+.btn-ghost:hover { color: var(--ink); background: var(--kraft); box-shadow: none; }
+.btn-ghost:active { background: var(--kraft-line); }
 
 .btn-danger { color: var(--pencil-red-dark); }
+.btn-danger:hover { color: #fff; background: var(--pencil-red-dark); border-color: var(--pencil-red-dark); }
+.btn-ghost.btn-danger:hover { color: var(--pencil-red-dark); background: rgba(172, 59, 42, 0.1); }
 
-.btn-sm { padding: 5px 10px; font-size: 12.5px; }
+.btn-sm { padding: 6px 11px; font-size: 12.5px; border-radius: var(--radius-xs); }
 
-.btn:disabled { opacity: 0.45; cursor: not-allowed; }
+.btn:disabled { opacity: 0.45; cursor: not-allowed; transform: none; box-shadow: none; }
 
 /* ---------- 本地文件夹连接按钮状态 ---------- */
 
@@ -274,18 +329,14 @@ button, input, textarea, select {
   border-color: #1a7f37;
   color: #fff;
 }
-.btn.fs-btn-connected:hover {
-  background: #1f8f41;
-}
+.btn.fs-btn-connected:hover { background: #1f8f41; border-color: #1f8f41; }
 
 .btn.fs-btn-need-perm {
   background: #9a6700;
   border-color: #9a6700;
   color: #fff;
 }
-.btn.fs-btn-need-perm:hover {
-  background: #b88500;
-}
+.btn.fs-btn-need-perm:hover { background: #b88500; border-color: #b88500; }
 
 .toolbar {
   display: flex;
@@ -297,41 +348,48 @@ button, input, textarea, select {
 
 .card-grid {
   display: grid;
-  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
-  gap: 16px;
+  grid-template-columns: repeat(auto-fill, minmax(266px, 1fr));
+  gap: 18px;
 }
 
 .plan-card {
   position: relative;
   background: var(--paper-card);
   border: 1px solid var(--border);
-  border-left: 4px solid var(--pencil-red);
+  border-left: 3px solid var(--pencil-red);
   border-radius: var(--radius-m);
-  padding: 16px 18px 14px;
+  padding: 18px 20px 15px;
   box-shadow: var(--shadow-card);
   display: flex;
   flex-direction: column;
-  gap: 8px;
+  gap: 9px;
   cursor: pointer;
-  transition: border-color .12s ease, box-shadow .12s ease, transform .12s ease;
+  transition: border-color .18s var(--ease), box-shadow .18s var(--ease), transform .18s var(--ease);
 }
 
 .plan-card:hover {
   border-color: var(--ink-green-700);
   border-left-color: var(--pencil-red);
-  box-shadow: 0 4px 14px rgba(43, 38, 32, 0.1);
-  transform: translateY(-2px);
+  box-shadow: var(--shadow-md);
+  transform: translateY(-3px);
 }
 
 .plan-card .plan-course {
-  font-size: 12px;
-  letter-spacing: 0.02em;
-  color: var(--ink-faint);
+  display: inline-flex;
+  align-self: flex-start;
+  font-size: 11px;
+  font-weight: 600;
+  letter-spacing: 0.03em;
+  color: var(--ink-green-800);
+  background: var(--kraft);
+  border-radius: var(--radius-pill);
+  padding: 3px 10px;
 }
 
 .plan-card h3 {
-  font-size: 17px;
-  margin: 0;
+  font-size: 17.5px;
+  line-height: 1.4;
+  margin: 2px 0 0;
 }
 
 .plan-card .plan-meta {
@@ -345,17 +403,24 @@ button, input, textarea, select {
 /* 教师 / 上课时间 / 学时：每一项单独一行，长短都不影响排版 */
 .plan-card .plan-meta-lines {
   flex-direction: column;
-  gap: 3px;
+  gap: 4px;
+}
+.plan-card .plan-meta-lines span { display: flex; align-items: center; gap: 6px; }
+
+.plan-card .plan-updated {
+  color: var(--ink-faint);
+  font-size: 11.5px;
 }
 
 .plan-card .plan-actions {
-  margin-top: 10px;
+  margin-top: 8px;
   display: flex;
   gap: 6px;
   flex-wrap: wrap;
-  border-top: 1px dashed var(--border);
-  padding-top: 10px;
+  border-top: 1px solid var(--border);
+  padding-top: 11px;
 }
+.plan-card .plan-actions .btn { box-shadow: none; }
 
 /* ---------- 资源管理器（文件夹） ---------- */
 
@@ -367,14 +432,15 @@ button, input, textarea, select {
   border: 1px solid var(--border);
   background: var(--paper-card);
   border-radius: var(--radius-m);
-  padding: 8px 14px;
-  margin-bottom: 18px;
+  padding: 10px 16px;
+  margin-bottom: 20px;
+  box-shadow: var(--shadow-xs);
 }
 
 .breadcrumb {
   display: flex;
   align-items: center;
-  gap: 6px;
+  gap: 4px;
   flex-wrap: wrap;
   font-size: 13.5px;
 }
@@ -383,9 +449,10 @@ button, input, textarea, select {
   background: transparent;
   color: var(--ink-soft);
   cursor: pointer;
-  padding: 2px 6px;
-  border-radius: 3px;
+  padding: 4px 8px;
+  border-radius: var(--radius-xs);
   font-size: 13.5px;
+  transition: background .14s var(--ease), color .14s var(--ease);
 }
 .crumb:hover { background: var(--kraft); color: var(--ink); }
 .crumb.active { color: var(--ink-green-800); font-weight: 600; }
@@ -400,31 +467,33 @@ button, input, textarea, select {
 }
 
 .breadcrumb-tools input[type="search"] {
-  width: 210px;
-  padding: 6px 10px;
+  width: 220px;
+  padding: 7px 12px;
   border: 1px solid var(--border);
-  border-radius: var(--radius-s);
+  border-radius: var(--radius-pill);
   background: var(--paper);
   font-size: 13px;
+  transition: border-color .14s var(--ease), box-shadow .14s var(--ease);
 }
 .breadcrumb-tools input[type="search"]:focus {
-  outline: 2px solid var(--ink-green-700);
-  outline-offset: -1px;
+  outline: none;
+  border-color: var(--ink-green-700);
+  box-shadow: 0 0 0 3px rgba(30, 74, 55, 0.12);
 }
 
 .explorer {
   display: flex;
-  gap: 20px;
+  gap: 22px;
   align-items: flex-start;
 }
 
 .explorer-sidebar {
-  width: 210px;
+  width: 214px;
   flex-shrink: 0;
   border: 1px solid var(--border);
   background: var(--paper-card);
   border-radius: var(--radius-m);
-  padding: 10px;
+  padding: 12px;
   display: flex;
   flex-direction: column;
   gap: 2px;
@@ -432,13 +501,14 @@ button, input, textarea, select {
   top: 16px;
   max-height: calc(100vh - 32px);
   overflow-y: auto;
+  box-shadow: var(--shadow-xs);
 }
 
 .xsidebar-label {
   font-size: 11px;
-  letter-spacing: 0.06em;
+  letter-spacing: 0.08em;
   color: var(--ink-faint);
-  padding: 10px 8px 4px;
+  padding: 12px 8px 5px;
 }
 
 .xsidebar-row {
@@ -446,28 +516,38 @@ button, input, textarea, select {
   align-items: center;
   gap: 2px;
   border-radius: var(--radius-s);
+  border-left: 3px solid transparent;
 }
-.xsidebar-row.active { background: var(--kraft); }
+.xsidebar-row.active { background: var(--kraft); border-left-color: var(--ink-green-700); }
 .xsidebar-row .xsidebar-item { flex: 1; min-width: 0; }
 
 .xsidebar-item {
   display: flex;
   align-items: center;
-  gap: 6px;
+  gap: 7px;
   border: none;
   background: transparent;
   color: var(--ink);
   text-align: left;
   font-size: 13.5px;
-  padding: 7px 8px;
+  padding: 8px 9px;
   border-radius: var(--radius-s);
   cursor: pointer;
   overflow: hidden;
   white-space: nowrap;
+  transition: background .14s var(--ease), color .14s var(--ease);
 }
 .xsidebar-item:hover { background: var(--kraft); }
 .xsidebar-item.active { background: var(--kraft); color: var(--ink-green-800); font-weight: 600; }
-.xsidebar-item .xcount { margin-left: auto; font-size: 11px; color: var(--ink-faint); }
+.xsidebar-item .xcount {
+  margin-left: auto;
+  font-size: 10.5px;
+  color: var(--ink-faint);
+  background: var(--paper);
+  border-radius: var(--radius-pill);
+  padding: 1px 7px;
+  flex-shrink: 0;
+}
 .xsidebar-item.active .xcount { color: var(--ink-green-700); }
 
 .xsidebar-actions { display: none; gap: 2px; }
@@ -478,19 +558,20 @@ button, input, textarea, select {
   color: var(--ink-faint);
   cursor: pointer;
   font-size: 12px;
-  padding: 2px 3px;
-  border-radius: 3px;
+  padding: 3px 5px;
+  border-radius: var(--radius-xs);
   line-height: 1;
+  transition: color .14s var(--ease), background .14s var(--ease);
 }
-.xsidebar-actions button:hover { color: var(--pencil-red-dark); }
+.xsidebar-actions button:hover { color: var(--pencil-red-dark); background: rgba(172, 59, 42, 0.08); }
 
 .xsidebar-add {
-  margin-top: 8px;
+  margin-top: 10px;
   border: 1px dashed var(--border);
   color: var(--ink-faint);
   justify-content: center;
 }
-.xsidebar-add:hover { color: var(--ink-green-800); border-color: var(--ink-green-700); }
+.xsidebar-add:hover { color: var(--ink-green-800); border-color: var(--ink-green-700); background: var(--kraft); }
 
 .explorer-main { flex: 1; min-width: 0; }
 
@@ -498,6 +579,7 @@ button, input, textarea, select {
 
 .folder-card {
   border-left-color: var(--ink-green-700);
+  align-items: center;
 }
 
 .folder-card:hover {
@@ -505,9 +587,10 @@ button, input, textarea, select {
 }
 
 .folder-card .folder-card-icon {
-  font-size: 32px;
+  font-size: 34px;
   text-align: center;
-  margin-bottom: 4px;
+  margin-bottom: 2px;
+  filter: drop-shadow(0 2px 3px rgba(22,40,31,0.15));
 }
 
 .folder-card h3 {
@@ -523,7 +606,7 @@ button, input, textarea, select {
 }
 
 /* 拖拽反馈 */
-.drag-over { outline: 2px dashed var(--ink-green-700); outline-offset: 2px; }
+.drag-over { outline: 2px dashed var(--ink-green-700); outline-offset: 2px; border-radius: var(--radius-m); }
 .plan-card.dragging { opacity: 0.45; }
 
 /* 文件夹命名弹窗输入框 */
@@ -562,20 +645,27 @@ button, input, textarea, select {
 .modal-backdrop {
   position: absolute;
   inset: 0;
-  background: rgba(22, 40, 31, 0.45);
+  background: rgba(22, 40, 31, 0.5);
+  backdrop-filter: blur(2px);
+}
+
+@keyframes modal-pop-in {
+  from { opacity: 0; transform: translateY(10px) scale(0.97); }
+  to { opacity: 1; transform: translateY(0) scale(1); }
 }
 
 .move-modal-content {
   position: relative;
   background: var(--paper-card);
   border: 1px solid var(--border);
-  border-radius: var(--radius-m);
-  box-shadow: 0 12px 40px rgba(0,0,0,0.25);
-  padding: 20px 22px;
+  border-radius: var(--radius-l);
+  box-shadow: var(--shadow-lg);
+  padding: 22px 24px;
   width: min(92vw, 360px);
   max-height: 80vh;
   display: flex;
   flex-direction: column;
+  animation: modal-pop-in 0.2s var(--ease);
 }
 .move-modal-content h3 {
   font-size: 16px;
@@ -598,10 +688,12 @@ button, input, textarea, select {
   font-size: 13.5px;
   color: var(--ink);
   cursor: pointer;
+  transition: border-color .14s var(--ease), background .14s var(--ease), transform .14s var(--ease);
 }
 .move-item:hover {
   border-color: var(--ink-green-700);
   background: var(--kraft);
+  transform: translateX(2px);
 }
 
 .move-modal-actions {
@@ -663,17 +755,19 @@ button, input, textarea, select {
 input[type="text"], input[type="date"], input[type="number"], input[type="url"], textarea {
   border: 1px solid var(--border);
   border-radius: var(--radius-s);
-  padding: 9px 10px;
+  padding: 9px 11px;
   background: var(--paper);
   width: 100%;
   resize: vertical;
+  transition: border-color .14s var(--ease), box-shadow .14s var(--ease);
 }
 
 textarea { min-height: 88px; line-height: 1.6; }
 
 input:focus, textarea:focus, select:focus {
-  outline: 2px solid var(--ink-green-700);
-  outline-offset: 1px;
+  outline: none;
+  border-color: var(--ink-green-700);
+  box-shadow: 0 0 0 3px rgba(30, 74, 55, 0.12);
 }
 
 /* 参考资料 / 内容大纲 的动态行 */
@@ -754,7 +848,7 @@ input:focus, textarea:focus, select:focus {
 .icon-btn {
   border: 1px solid var(--border);
   background: var(--paper-card);
-  border-radius: 3px;
+  border-radius: var(--radius-xs);
   width: 26px;
   height: 26px;
   display: flex;
@@ -764,8 +858,9 @@ input:focus, textarea:focus, select:focus {
   font-size: 12px;
   line-height: 1;
   color: var(--ink-soft);
+  transition: border-color .14s var(--ease), color .14s var(--ease), background .14s var(--ease);
 }
-.icon-btn:hover { border-color: var(--ink-green-700); color: var(--ink-green-800); }
+.icon-btn:hover { border-color: var(--ink-green-700); color: var(--ink-green-800); background: var(--kraft); }
 
 .add-row-btn {
   margin-top: 4px;
@@ -830,56 +925,66 @@ input:focus, textarea:focus, select:focus {
   position: fixed;
   bottom: 60px;
   right: 24px;
-  width: 46px;
-  height: 46px;
+  width: 50px;
+  height: 50px;
   border-radius: 50%;
-  background: var(--ink-green-800);
+  background: linear-gradient(155deg, var(--ink-green-700), var(--ink-green-900));
   color: #fff;
   border: none;
   cursor: pointer;
   display: flex;
   align-items: center;
   justify-content: center;
-  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.2);
+  font-size: 19px;
+  box-shadow: var(--shadow-md);
   z-index: 99;
-  transition: transform 0.2s, box-shadow 0.2s;
+  transition: transform 0.2s var(--ease), box-shadow 0.2s var(--ease);
 }
 
 .ai-fab:hover {
-  transform: scale(1.08);
-  box-shadow: 0 6px 20px rgba(0, 0, 0, 0.25);
+  transform: scale(1.08) translateY(-2px);
+  box-shadow: var(--shadow-lg);
 }
+.ai-fab:active { transform: scale(1.02); }
 
 .ai-fab-badge {
   position: absolute;
-  top: -2px;
-  right: -2px;
-  width: 12px;
-  height: 12px;
+  top: -1px;
+  right: -1px;
+  width: 13px;
+  height: 13px;
   background: var(--pencil-red);
   border-radius: 50%;
   border: 2px solid #fff;
+  box-shadow: 0 0 0 0 rgba(196, 68, 46, 0.5);
+  animation: ai-badge-pulse 2.4s infinite;
+}
+@keyframes ai-badge-pulse {
+  0% { box-shadow: 0 0 0 0 rgba(196, 68, 46, 0.45); }
+  70% { box-shadow: 0 0 0 6px rgba(196, 68, 46, 0); }
+  100% { box-shadow: 0 0 0 0 rgba(196, 68, 46, 0); }
 }
 
 /* 聊天窗口 */
 .ai-chat {
   position: fixed;
-  bottom: 120px;
+  bottom: 122px;
   right: 24px;
   width: 380px;
   max-height: 600px;
   background: var(--paper);
-  border-radius: var(--radius-m);
-  box-shadow: 0 8px 30px rgba(0, 0, 0, 0.18);
+  border: 1px solid var(--border);
+  border-radius: var(--radius-l);
+  box-shadow: var(--shadow-lg);
   z-index: 100;
   display: flex;
   flex-direction: column;
   overflow: hidden;
-  animation: ai-chat-in 0.25s ease-out;
+  animation: ai-chat-in 0.22s var(--ease);
 }
 
 @keyframes ai-chat-in {
-  from { opacity: 0; transform: translateY(12px) scale(0.96); }
+  from { opacity: 0; transform: translateY(14px) scale(0.96); }
   to { opacity: 1; transform: translateY(0) scale(1); }
 }
 
@@ -888,18 +993,29 @@ input:focus, textarea:focus, select:focus {
   display: flex;
   align-items: center;
   justify-content: space-between;
-  padding: 12px 16px;
-  background: var(--ink-green-700);
+  padding: 13px 16px;
+  background: linear-gradient(120deg, var(--ink-green-800), var(--ink-green-700));
   color: #fff;
 }
 
 .ai-chat-title {
   display: flex;
   align-items: center;
-  gap: 8px;
+  gap: 9px;
   font-weight: 600;
   font-size: 14px;
 }
+.ai-chat-title::before {
+  content: "✦";
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  width: 22px;
+  height: 22px;
+  border-radius: 50%;
+  background: rgba(255,255,255,0.16);
+  font-size: 11px;
+}
 
 .ai-chat-tools {
   display: flex;
@@ -910,18 +1026,19 @@ input:focus, textarea:focus, select:focus {
   width: 28px;
   height: 28px;
   border: none;
-  background: rgba(255, 255, 255, 0.15);
+  background: rgba(255, 255, 255, 0.14);
   color: #fff;
-  border-radius: 4px;
+  border-radius: var(--radius-xs);
   cursor: pointer;
   display: flex;
   align-items: center;
   justify-content: center;
-  transition: background 0.15s;
+  transition: background .16s var(--ease), transform .16s var(--ease);
 }
+.ai-chat-tool:active { transform: scale(0.92); }
 
 .ai-chat-tool:hover {
-  background: rgba(255, 255, 255, 0.28);
+  background: rgba(255, 255, 255, 0.26);
 }
 
 /* 快捷功能 */
@@ -929,47 +1046,49 @@ input:focus, textarea:focus, select:focus {
   display: flex;
   flex-wrap: wrap;
   gap: 6px;
-  padding: 10px 12px;
+  padding: 11px 12px;
   border-bottom: 1px solid var(--border);
   background: var(--kraft);
 }
 
 .ai-quick-btn {
-  padding: 5px 10px;
+  padding: 5px 11px;
   font-size: 12px;
   background: var(--paper);
   border: 1px solid var(--border);
-  border-radius: 14px;
+  border-radius: var(--radius-pill);
   cursor: pointer;
   color: var(--ink);
-  transition: all 0.15s;
+  transition: background .16s var(--ease), border-color .16s var(--ease), color .16s var(--ease), transform .16s var(--ease);
 }
 
 .ai-quick-btn:hover {
   background: var(--ink-green-700);
   border-color: var(--ink-green-700);
   color: #fff;
+  transform: translateY(-1px);
 }
 
 /* 消息区域 */
 .ai-messages {
   flex: 1;
   overflow-y: auto;
-  padding: 12px;
+  padding: 14px 12px;
   min-height: 120px;
   max-height: 280px;
   display: flex;
   flex-direction: column;
-  gap: 10px;
+  gap: 11px;
 }
 
 .ai-message {
   max-width: 88%;
-  padding: 8px 12px;
-  border-radius: 12px;
+  padding: 9px 13px;
+  border-radius: var(--radius-m);
   font-size: 13px;
   line-height: 1.6;
   word-break: break-word;
+  box-shadow: var(--shadow-xs);
 }
 
 .ai-message-user {
@@ -977,6 +1096,7 @@ input:focus, textarea:focus, select:focus {
   background: var(--ink-green-700);
   color: #fff;
   border-bottom-right-radius: 4px;
+  box-shadow: none;
 }
 
 .ai-message-ai {
@@ -988,9 +1108,11 @@ input:focus, textarea:focus, select:focus {
 
 .ai-message-error {
   align-self: flex-start;
-  background: #fee;
-  color: #c33;
+  background: rgba(196, 68, 46, 0.08);
+  border: 1px solid rgba(196, 68, 46, 0.25);
+  color: var(--pencil-red-dark);
   border-bottom-left-radius: 4px;
+  box-shadow: none;
 }
 
 .ai-message-content {
@@ -1000,17 +1122,18 @@ input:focus, textarea:focus, select:focus {
 .ai-message-actions {
   display: flex;
   gap: 6px;
-  margin-top: 6px;
+  margin-top: 7px;
 }
 
 .ai-message-actions button {
   font-size: 11px;
-  padding: 3px 8px;
-  border-radius: 4px;
+  padding: 3px 9px;
+  border-radius: var(--radius-pill);
   border: 1px solid var(--border);
   background: var(--paper);
   cursor: pointer;
   color: var(--ink-soft);
+  transition: background .14s var(--ease), color .14s var(--ease), border-color .14s var(--ease);
 }
 
 .ai-message-actions button:hover {
@@ -1031,10 +1154,10 @@ input:focus, textarea:focus, select:focus {
   flex: 1;
   min-width: 0;
   border: 1px solid var(--border);
-  border-radius: 3px;
+  border-radius: var(--radius-xs);
   background: #fff;
   font-size: 12px;
-  padding: 4px 6px;
+  padding: 5px 7px;
   color: var(--ink);
 }
 
@@ -1043,10 +1166,11 @@ input:focus, textarea:focus, select:focus {
   border: none;
   background: var(--ink-green-800);
   color: #fff;
-  border-radius: 3px;
+  border-radius: var(--radius-xs);
   padding: 5px 12px;
   font-size: 12px;
   cursor: pointer;
+  transition: background .14s var(--ease);
 }
 
 .ai-fill-btn:hover { background: var(--ink-green-700); }
@@ -1056,7 +1180,7 @@ input:focus, textarea:focus, select:focus {
   display: flex;
   align-items: flex-end;
   gap: 8px;
-  padding: 10px 12px;
+  padding: 11px 12px;
   border-top: 1px solid var(--border);
   background: var(--paper-card);
 }
@@ -1065,18 +1189,20 @@ input:focus, textarea:focus, select:focus {
   flex: 1;
   resize: none;
   border: 1px solid var(--border);
-  border-radius: var(--radius-s);
-  padding: 8px 10px;
+  border-radius: var(--radius-m);
+  padding: 8px 11px;
   font-size: 13px;
   line-height: 1.5;
   min-height: 36px;
   max-height: 80px;
   font-family: inherit;
+  transition: border-color .14s var(--ease), box-shadow .14s var(--ease);
 }
 
 .ai-input-area textarea:focus {
-  outline: 2px solid var(--ink-green-700);
-  outline-offset: -1px;
+  outline: none;
+  border-color: var(--ink-green-700);
+  box-shadow: 0 0 0 3px rgba(30, 74, 55, 0.12);
 }
 
 .ai-send-btn {
@@ -1091,16 +1217,21 @@ input:focus, textarea:focus, select:focus {
   align-items: center;
   justify-content: center;
   flex-shrink: 0;
-  transition: background 0.15s;
+  box-shadow: var(--shadow-xs);
+  transition: background 0.16s var(--ease), transform 0.16s var(--ease), box-shadow 0.16s var(--ease);
 }
 
 .ai-send-btn:hover {
   background: var(--ink-green-800);
+  transform: translateY(-1px);
+  box-shadow: var(--shadow-sm);
 }
 
 .ai-send-btn:disabled {
   opacity: 0.5;
   cursor: not-allowed;
+  transform: none;
+  box-shadow: none;
 }
 
 /* 加载动画 */
@@ -1139,21 +1270,24 @@ input:focus, textarea:focus, select:focus {
 .ai-modal-backdrop {
   position: absolute;
   inset: 0;
-  background: rgba(0, 0, 0, 0.5);
+  background: rgba(22, 40, 31, 0.5);
+  backdrop-filter: blur(2px);
 }
 
 .ai-modal-content {
   position: relative;
   background: var(--paper);
-  border-radius: var(--radius-m);
-  padding: 24px;
+  border: 1px solid var(--border);
+  border-radius: var(--radius-l);
+  padding: 26px;
   width: 90%;
   max-width: 440px;
-  box-shadow: 0 8px 30px rgba(0, 0, 0, 0.2);
+  box-shadow: var(--shadow-lg);
+  animation: modal-pop-in 0.2s var(--ease);
 }
 
 .ai-modal-content h3 {
-  margin-bottom: 16px;
+  margin-bottom: 18px;
 }
 
 .ai-modal-content .field {
@@ -1163,7 +1297,7 @@ input:focus, textarea:focus, select:focus {
 .ai-setup-notice {
   background: var(--kraft);
   border: 1px solid var(--kraft-line);
-  border-radius: var(--radius-s);
+  border-radius: var(--radius-m);
   padding: 14px 16px;
   margin-bottom: 18px;
   font-size: 13px;
@@ -1192,7 +1326,7 @@ input:focus, textarea:focus, select:focus {
   display: flex;
   justify-content: flex-end;
   gap: 8px;
-  margin-top: 20px;
+  margin-top: 22px;
 }
 
 .ai-test-row {
@@ -1261,13 +1395,13 @@ input:focus, textarea:focus, select:focus {
   bottom: 24px;
   background: var(--ink-green-900);
   color: #fff;
-  padding: 10px 16px;
-  border-radius: var(--radius-s);
+  padding: 11px 18px;
+  border-radius: var(--radius-m);
   font-size: 13px;
-  box-shadow: 0 6px 20px rgba(0,0,0,0.25);
+  box-shadow: var(--shadow-lg);
   opacity: 0;
   transform: translateY(6px);
-  transition: opacity .18s ease, transform .18s ease;
+  transition: opacity .2s var(--ease), transform .2s var(--ease);
   pointer-events: none;
   z-index: 50;
 }
diff --git a/js/dashboard.js b/js/dashboard.js
index 2881a12..d1f004d 100644
--- a/js/dashboard.js
+++ b/js/dashboard.js
@@ -90,11 +90,11 @@ function render(keyword = "") {
       <div class="plan-course">${escapeHtml(plan.courseName || "未分类课程")}${plan.courseCategory ? " · " + escapeHtml(plan.courseCategory) : ""}</div>
       <h3>${escapeHtml(plan.lessonTitle || "（未命名课题）")}</h3>
       <div class="plan-meta plan-meta-lines">
-        <span>教师：${escapeHtml(plan.teacher || "—")}</span>
-        <span>上课时间：${escapeHtml(plan.teachDate || "—")}</span>
-        <span>学时：${escapeHtml(String(plan.hours || "1"))}</span>
+        <span>👤 ${escapeHtml(plan.teacher || "—")}</span>
+        <span>🕐 ${escapeHtml(plan.teachDate || "—")}</span>
+        <span>⏱ ${escapeHtml(String(plan.hours || "1"))} 学时</span>
       </div>
-      <div class="plan-meta" style="color:var(--ink-faint)">更新于 ${fmtDate(plan.updatedAt)}</div>
+      <div class="plan-meta plan-updated">更新于 ${fmtDate(plan.updatedAt)}</div>
       <div class="plan-actions">
         <a class="btn btn-sm btn-primary" href="editor.html?id=${encodeURIComponent(plan.id)}">编辑</a>
         <button class="btn btn-sm" data-act="dup" data-id="${plan.id}">复制</button>
